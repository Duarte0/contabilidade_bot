# =================================================================
# CONFIGURAÇÃO DO SISTEMA DE COBRANÇAS E DOCUMENTOS
# =================================================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env: cp .env.example .env
# 2. Preencha com suas credenciais reais
# 3. NUNCA commite o arquivo .env no Git!
#
# =================================================================

# -----------------------------------------------------------------
# DIGISAC API - INTEGRAÇÃO WHATSAPP
# -----------------------------------------------------------------
# Token de autenticação da API Digisac
# Obtenha em: https://app.digisac.app/configuracoes/api
DIGISAC_API_TOKEN=seu_token_digisac_aqui

# URL base da API Digisac
DIGISAC_API_URL=https://dominio.digisac.chat/api/v1

# URL do webhook (para receber notificações)
DIGISAC_WEBHOOK_URL=https://seu-webhook.ngrok-free.dev

# -----------------------------------------------------------------
# BANCO DE DADOS - POSTGRESQL
# -----------------------------------------------------------------
# Nome do banco de dados
POSTGRES_DB=cobranca_db

# Usuário do banco
POSTGRES_USER=postgres

# Senha do banco (MUDE ESTA SENHA EM PRODUÇÃO!)
POSTGRES_PASSWORD=sua_senha_postgres

# Host do banco
# Use 'postgres' quando rodar via Docker Compose
# Use 'localhost' quando rodar localmente
POSTGRES_HOST=postgres

# Porta do banco
POSTGRES_PORT=5432

# -----------------------------------------------------------------
# API BACKEND - FASTAPI
# -----------------------------------------------------------------
# URL base da API backend
API_URL=http://localhost:8000

# Ambiente (development, production)
ENVIRONMENT=development

# Secret key para JWT (gere uma chave aleatória segura)
# Você pode gerar com: openssl rand -hex 32
SECRET_KEY=sua_chave_secreta_muito_longa_e_aleatoria_aqui

# Debug mode (desabilite em produção!)
DEBUG=true

# -----------------------------------------------------------------
# FRONTEND
# -----------------------------------------------------------------
# URL do frontend para CORS
FRONTEND_URL=http://localhost:3000

# -----------------------------------------------------------------
# REDIS (OPCIONAL - Para processamento em background)
# -----------------------------------------------------------------
# Host do Redis
REDIS_HOST=redis

# Porta do Redis
REDIS_PORT=6379

# Senha do Redis (deixe vazio se não tiver)
REDIS_PASSWORD=

# -----------------------------------------------------------------
# CONFIGURAÇÕES AVANÇADAS
# -----------------------------------------------------------------
# Nível de log (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Timezone
TZ=America/Sao_Paulo

# Número máximo de workers (para Docker)
WORKERS=2

# -----------------------------------------------------------------
# SEGURANÇA
# -----------------------------------------------------------------
# Permitir CORS de todas as origens (não use em produção!)
ALLOW_ALL_ORIGINS=false

# Origens permitidas (separadas por vírgula)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# -----------------------------------------------------------------
# LIMITES E QUOTAS
# -----------------------------------------------------------------
# Limite de mensagens por minuto
RATE_LIMIT_PER_MINUTE=60

# Limite de clientes por lote
MAX_BATCH_SIZE=100

# Timeout para envio (segundos)
SEND_TIMEOUT=30

# -----------------------------------------------------------------
# DESENVOLVIMENTO LOCAL (SEM DOCKER)
# -----------------------------------------------------------------
# Se estiver rodando localmente sem Docker, altere:
# POSTGRES_HOST=localhost
# REDIS_HOST=localhost